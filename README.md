# Risk Engine — Playable Slice v0.1

This repository contains a deterministic strategy simulation engine designed as a playable slice.
The system emphasizes explicit phase boundaries, persisted state, and governance constraints.

## Requirements
- Python 3.x
- Windows (for `RUN_RISK_ENGINE.bat`), or Python CLI for manual execution

## How to Run (Windows)
1. Download or clone this repository
2. Double-click `RUN_RISK_ENGINE.bat`

## How to Run (Manual)
From the repository root:

```bash
python main.py
```
